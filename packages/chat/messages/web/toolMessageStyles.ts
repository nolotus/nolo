import * as stylex from "@stylexjs/stylex";

const slideIn = stylex.keyframes({
  from: { opacity: 0, transform: "translateY(-8px)" },
  to: { opacity: 1, transform: "translateY(0)" },
});

/** StyleX-only styles for the ToolMessageItem shell. Keep legacy class names as test/DOM anchors. */
export const toolMessageStyles = stylex.create({
  row: {
    position: "relative",
    width: "100%",
    maxWidth: 820,
    marginTop: 0,
    marginRight: 0,
    marginBottom: 0,
    marginLeft: 60,
    transition: "all 0.18s ease",
    borderTopLeftRadius: "var(--radius-sm, 8px)", borderTopRightRadius: "var(--radius-sm, 8px)", borderBottomRightRadius: "var(--radius-sm, 8px)", borderBottomLeftRadius: "var(--radius-sm, 8px)",
    backgroundColor: "transparent",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "transparent",
    overflow: "hidden",
  },
  rowCollapsed: { backgroundColor: "transparent", borderColor: "transparent" },
  rowHandoff: { maxWidth: 720 },
  rowAppDeploy: { maxWidth: "min(1480px, calc(100vw - 108px))" },
  header: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "flex-start",
    paddingTop: 8,
    paddingRight: 12,
    paddingBottom: 8,
    paddingLeft: 12,
    cursor: "pointer",
    transition: "background 0.18s ease, border-color 0.18s ease",
    userSelect: "none",
    minHeight: "var(--control-sm)",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "transparent",
    backgroundColor: "transparent",
  },
  headerToggle: {
    width: "auto",
    flex: 1,
    minWidth: 0,
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: 6,
    paddingTop: 0,
    paddingRight: 0,
    paddingBottom: 0,
    paddingLeft: 0,
    borderWidth: 0,
    borderStyle: "none",
    cursor: "pointer",
  },
  main: {
    display: "flex",
    alignItems: "flex-start",
    gap: 10,
    flex: 1,
    minWidth: 0,
  },
  icon: {
    position: "relative",
    width: 18,
    height: 18,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: 14,
    marginTop: 1,
  },
  // Astryx ChatToolCalls status dot: 15%-tinted status-colored circle behind a
  // status-colored glyph. Tone comes from the icon-* color class on the same
  // element; the tint uses currentColor at 0.15 opacity.
  statusDot: {
    position: "relative",
    width: 16,
    height: 16,
    borderRadius: "50%",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0,
    marginTop: 1,
  },
  statusDotBg: {
    position: "absolute",
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    borderRadius: "inherit",
    backgroundColor: "currentColor",
    opacity: 0.15,
  },
  statusDotGlyph: {
    position: "relative",
  },
  // Trailing duration badge on completed rows, e.g. `1.2s` / `850ms`. Also
  // styles the ToolCallRow diff meta segment (mono + never shrinks).
  duration: {
    fontFamily: "var(--font-mono, monospace)",
    fontSize: 11,
    color: "var(--textQuaternary)",
    whiteSpace: "nowrap",
    flexShrink: 0,
    paddingTop: 2,
  },
  summary: {
    fontSize: "var(--fontSize-sm)",
    fontWeight: 500,
    color: "var(--textMuted, var(--textSecondary))",
    flex: 1,
    opacity: 0.9,
    transition: "color 0.18s ease, opacity 0.18s ease",
  },
  chevron: {
    color: "var(--textQuaternary)",
    display: "flex",
    opacity: 0.6,
    marginTop: 1,
    transition: "transform 0.18s ease, opacity 0.18s ease",
  },
  body: {
    maxHeight: "min(52vh, 560px)",
    overflowY: "auto",
    overscrollBehavior: "contain",
    marginTop: 10,
    marginRight: 0,
    marginBottom: 14,
    marginLeft: 18,
    paddingTop: 0,
    paddingRight: 2,
    paddingBottom: 0,
    paddingLeft: 16,
    borderLeftWidth: 1,
    borderLeftStyle: "solid",
    borderLeftColor: "var(--borderMuted, var(--borderSubtle))",
    animationName: slideIn,
    animationDuration: "0.3s",
    animationTimingFunction: "cubic-bezier(0.4, 0, 0.2, 1)",
  },
  handoffBody: { display: "grid", gap: 8, maxWidth: 640 },
  bodyAppDeploy: {
    marginTop: 10,
    marginRight: 0,
    marginBottom: 14,
    marginLeft: 0,
    paddingTop: 0,
    paddingRight: 0,
    paddingBottom: 0,
    paddingLeft: 0,
    borderLeftWidth: 0,
    borderLeftStyle: "none",
    maxHeight: "none",
    overflowY: "visible",
    animationName: slideIn,
    animationDuration: "0.3s",
    animationTimingFunction: "cubic-bezier(0.4, 0, 0.2, 1)",
  },
  handoffDetailRow: {
    display: "grid",
    gridTemplateColumns: "112px minmax(0, 1fr)",
    alignItems: "start",
    gap: 10,
    fontSize: "var(--fontSize-sm)",
    lineHeight: "var(--leading-normal)",
  },
  handoffLabel: {
    color: "var(--textQuaternary, var(--textTertiary))",
    whiteSpace: "nowrap",
  },
  handoffValue: {
    minWidth: 0,
    color: "var(--textSecondary)",
    overflowWrap: "anywhere",
  },
  handoffLink: {
    display: "inline-flex",
    width: "fit-content",
    alignItems: "center",
    gap: 6,
    minWidth: 0,
    paddingTop: 0,
    paddingRight: 0,
    paddingBottom: 0,
    paddingLeft: 0,
    borderWidth: 0,
    borderStyle: "none",
    backgroundColor: "transparent",
    color: "var(--primary)",
    cursor: "pointer",
    fontFamily: "inherit",
    fontSize: "inherit",
    fontWeight: "inherit",
    lineHeight: "inherit",
    textDecoration: "none",
  },
  actions: { display: "flex", alignItems: "center", gap: 6 },
  actionBar: {
    display: "flex",
    gap: 2,
    opacity: 0,
    transform: "translateX(4px)",
    transition: "opacity 0.18s ease, transform 0.18s ease",
  },
  actionButton: {
    width: 24,
    height: 24,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "transparent",
    backgroundColor: "transparent",
    color: "var(--textQuaternary)",
    borderTopLeftRadius: "var(--radius-xs)", borderTopRightRadius: "var(--radius-xs)", borderBottomRightRadius: "var(--radius-xs)", borderBottomLeftRadius: "var(--radius-xs)",
    cursor: "pointer",
  },
  actionButtonOn: { color: "var(--primary)", backgroundColor: "var(--primaryGhost, transparent)" },
  confirmBanner: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    paddingTop: 10,
    paddingRight: 14,
    paddingBottom: 10,
    paddingLeft: 14,
    marginBottom: 12,
    backgroundColor:
      "var(--surfaceInset, var(--surfaceRaised, var(--backgroundSecondary)))",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "var(--borderMuted, var(--borderAccent, var(--borderLight)))",
    borderTopLeftRadius: "var(--radius-sm)", borderTopRightRadius: "var(--radius-sm)", borderBottomRightRadius: "var(--radius-sm)", borderBottomLeftRadius: "var(--radius-sm)",
  },
  confirmText: {
    fontSize: "var(--fontSize-sm)",
    fontWeight: 500,
    color: "var(--textSecondary)",
  },
  primaryButton: {
    paddingTop: 6,
    paddingRight: 12,
    paddingBottom: 6,
    paddingLeft: 12,
    fontSize: "var(--fontSize-sm)",
    fontWeight: 600,
    borderTopLeftRadius: 999, borderTopRightRadius: 999, borderBottomRightRadius: 999, borderBottomLeftRadius: 999,
    backgroundColor: "var(--accentSoft, var(--primaryGhost))",
    color: "var(--primary)",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "color-mix(in srgb, var(--primary) 18%, transparent)",
    cursor: "pointer",
    transition:
      "background 0.18s ease, border-color 0.18s ease, transform 0.18s ease",
  },
  debugBox: {
    marginTop: 8,
    marginRight: 0,
    marginBottom: 8,
    marginLeft: 18,
    paddingTop: 12,
    paddingRight: 12,
    paddingBottom: 12,
    paddingLeft: 12,
    backgroundColor: "var(--surfaceCode, var(--codeBackground))",
    borderTopLeftRadius: "var(--radius-xs)", borderTopRightRadius: "var(--radius-xs)", borderBottomRightRadius: "var(--radius-xs)", borderBottomLeftRadius: "var(--radius-xs)",
    fontFamily: "var(--font-mono, monospace)",
    fontSize: "var(--fontSize-sm)",
    color: "var(--textTertiary)",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "var(--borderMuted, var(--borderLight))",
    overflow: "auto",
    lineHeight: "var(--leading-normal)",
    maxHeight: "min(44vh, 420px)",
    overscrollBehavior: "contain",
  },
  rowFailed: {},
  rowRunning: {},
  rowRepairing: {},
  failedHeader: {
    backgroundColor: "color-mix(in srgb, var(--error) 6%, transparent)",
  },
  repairingHeader: {
    backgroundColor: "color-mix(in srgb, var(--warning) 7%, transparent)",
    borderColor: "color-mix(in srgb, var(--warning) 18%, transparent)",
  },
  failedSummary: { color: "var(--error)", opacity: 1 },
  repairingSummary: { color: "var(--warning)", opacity: 1 },
  successSummary: { color: "var(--textSecondary)", opacity: 1 },
  runningSummary: { color: "var(--primary)", opacity: 1 },
  repairingIcon: { color: "var(--warning)" },
  failedBody: {
    borderLeftColor: "color-mix(in srgb, var(--error) 30%, transparent)",
  },
  repairingBody: {
    borderLeftColor: "color-mix(in srgb, var(--warning) 30%, transparent)",
  },
  stepProgress: {
    display: "flex",
    flexDirection: "column",
    gap: "var(--space-2)",
    paddingTop: "var(--space-2)",
    paddingRight: "var(--space-3)",
    paddingBottom: "var(--space-2)",
    paddingLeft: "var(--space-3)",
    marginBottom: "var(--space-2)",
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "var(--borderMuted, var(--borderLight))",
    borderTopLeftRadius: "var(--radius-sm)", borderTopRightRadius: "var(--radius-sm)", borderBottomRightRadius: "var(--radius-sm)", borderBottomLeftRadius: "var(--radius-sm)",
    backgroundColor:
      "var(--surfaceInset, var(--surfaceRaised, var(--backgroundSecondary)))",
  },
  actionList: {
    display: "flex",
    flexDirection: "column",
    gap: 4,
    marginLeft: 26,
    overflow: "hidden",
  },
  // Flat group body/list (P1): ordinary tool-call groups drop the timeline
  // connector — no left border, no big indent. Named-phase groups keep
  // `groupBody` + `actionList` above/below.
  groupBodyFlat: {
    maxHeight: "min(32vh, 280px)",
    overflowY: "auto",
    overscrollBehavior: "contain",
    display: "flex",
    flexDirection: "column",
    gap: 2,
    paddingTop: 4,
    paddingRight: 0,
    paddingBottom: 8,
    paddingLeft: 0,
  },
  flatList: {
    display: "flex",
    flexDirection: "column",
    gap: 2,
    minWidth: 0,
    overflow: "hidden",
  },
  action: { position: "relative", minWidth: 0 },
  actionRow: {
    width: "100%",
    minWidth: 0,
    display: "flex",
    alignItems: "center",
    gap: 8,
    paddingTop: 3,
    paddingRight: 6,
    paddingBottom: 3,
    paddingLeft: 6,
    borderWidth: 0,
    borderStyle: "none",
    borderTopLeftRadius: "var(--radius-xs)", borderTopRightRadius: "var(--radius-xs)", borderBottomRightRadius: "var(--radius-xs)", borderBottomLeftRadius: "var(--radius-xs)",
    color: "var(--textMuted, var(--textSecondary))",
    backgroundColor: "transparent",
    cursor: "pointer",
    textAlign: "left",
  },
  actionDot: {
    width: 7,
    height: 7,
    flexShrink: 0,
    borderTopLeftRadius: 999, borderTopRightRadius: 999, borderBottomRightRadius: 999, borderBottomLeftRadius: 999,
    backgroundColor: "var(--borderHover, var(--borderSubtle))",
  },
  actionDotSuccess: { backgroundColor: "#10b981" },
  actionDotRunning: {
    backgroundColor: "var(--primary)",
    boxShadow: "0 0 0 3px color-mix(in srgb, var(--primary) 15%, transparent)",
  },
  actionDotFailed: { backgroundColor: "var(--error)" },
  actionLabel: {
    flex: 1,
    minWidth: 0,
    fontSize: "var(--fontSize-sm)",
    fontWeight: 500,
  },
  actionChevron: {
    display: "flex",
    flexShrink: 0,
    color: "var(--textQuaternary, var(--textTertiary))",
    opacity: 0.8,
  },
  // ToolCallRow flex contract (P1): verb keeps intrinsic width (flexShrink 0);
  // the target is the MAIN flexible column (flex:1 + minWidth:0, mono so the
  // operand reads as primary content); context is the weakest signal —
  // explicitly shrinkable (minWidth:0 + flexShrink:1) under a hard max-width
  // cap. Real clipping (nowrap/hidden/ellipsis) comes from the shared
  // `truncate` entry mounted alongside it, so a long no-space path shrinks
  // inside its own box instead of pushing duration/chevron out of the header.
  // Narrow viewports shrink the context cap further and finally hide it —
  // the row must never overflow horizontally (rowContext media queries).
  rowVerb: {
    flexShrink: 0,
    fontSize: "var(--fontSize-sm)",
    fontWeight: 500,
  },
  rowContext: {
    minWidth: 0,
    flexShrink: 1,
    maxWidth: 240,
    fontSize: 12,
    color: "var(--textQuaternary, var(--textTertiary))",
    "@media (max-width: 640px)": { maxWidth: 140 },
    "@media (max-width: 460px)": { display: "none" },
  },
  rowTarget: {
    flex: 1,
    minWidth: 0,
    fontFamily: "var(--font-mono, monospace)",
  },
  // Flat row anatomy (P1): 28–32px density, transparent on the flat group
  // body. Light hover + focus-visible ring live in
  // messagesStylexEscapeHatch.css under the messages-esc-tool-call-row-header
  // hook — a dedicated selector the legacy tr-action-row hover rules
  // (messages-esc-tr-action-row) never match, so the timeline hover cannot
  // override the flat row's own states.
  rowHeader: {
    width: "100%",
    minWidth: 0,
    display: "flex",
    alignItems: "center",
    gap: 8,
    minHeight: 30,
    paddingTop: 4,
    paddingRight: 6,
    paddingBottom: 4,
    paddingLeft: 6,
    borderWidth: 0,
    borderStyle: "none",
    borderTopLeftRadius: "var(--radius-xs)", borderTopRightRadius: "var(--radius-xs)", borderBottomRightRadius: "var(--radius-xs)", borderBottomLeftRadius: "var(--radius-xs)",
    color: "var(--textMuted, var(--textSecondary))",
    backgroundColor: "transparent",
    cursor: "pointer",
    textAlign: "left",
  },
  // Expanded flat row body: light indent only — the named-phase timeline
  // keeps its actionDetail chrome (P1 flat/named split).
  rowDetail: {
    marginTop: 2,
    marginRight: 0,
    marginBottom: 6,
    marginLeft: 24,
    paddingTop: 0,
    paddingRight: 4,
    paddingBottom: 0,
    paddingLeft: 0,
    color: "var(--textSecondary)",
  },
  actionDetail: {
    marginTop: 4,
    marginRight: 0,
    marginBottom: 8,
    marginLeft: 15,
    paddingTop: 0,
    paddingRight: 0,
    paddingBottom: 0,
    paddingLeft: 12,
    color: "var(--textSecondary)",
  },
  groupBody: {
    maxHeight: "min(32vh, 280px)",
    overflowY: "auto",
    overscrollBehavior: "contain",
    display: "flex",
    flexDirection: "column",
    gap: 4,
    marginLeft: 18,
    paddingTop: 6,
    paddingRight: 0,
    paddingBottom: 8,
    paddingLeft: 16,
    borderLeftWidth: 1,
    borderLeftStyle: "solid",
    borderLeftColor: "var(--borderMuted, var(--borderSubtle))",
  },
  groupItem: { marginTop: 0, marginRight: 0, marginBottom: 0, marginLeft: 0 },
  phaseList: { display: "flex", flexDirection: "column", gap: 4 },
  phaseRow: { width: "100%", display: "flex", alignItems: "flex-start", gap: 9, minWidth: 0, paddingTop: 0, paddingRight: 0, paddingBottom: 0, paddingLeft: 0, borderWidth: 0, borderStyle: "none", backgroundColor: "transparent", color: "inherit", textAlign: "left", cursor: "pointer" },
  phaseMain: {
    display: "flex",
    gap: 8,
    flex: 1,
    minWidth: 0,
    alignItems: "baseline" },
  errorText: { color: "var(--error)" },
  truncate: { whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" },
  phaseTitle: { minWidth: 0, fontSize: "var(--fontSize-sm)", fontWeight: 600 },
  phaseMeta: {
    flexShrink: 0,
    fontSize: "var(--fontSize-xs)",
    color: "var(--textQuaternary)",
  },
  phaseChevron: { display: "inline-flex", alignItems: "center", justifyContent: "center", color: "var(--textQuaternary, var(--textTertiary))", paddingTop: 1 },
  phaseStatusRunning: { color: "var(--primary)" },
  phaseStatusFailed: { color: "var(--error)" },
  phaseStatus: { position: "relative", zIndex: 1, width: 17, height: 17, flexShrink: 0, display: "flex", alignItems: "center", justifyContent: "center", color: "var(--textQuaternary, var(--textTertiary))", backgroundColor: "var(--backgroundPrimary, var(--background))", borderRadius: 999 },
});

export type ToolStatusStyleGroup = Partial<{
  row: stylex.StyleXStyles;
  header: stylex.StyleXStyles;
  summary: stylex.StyleXStyles;
  body: stylex.StyleXStyles;
  icon: stylex.StyleXStyles;
}>;

export const toolMessageStatusStyles: Record<string, ToolStatusStyleGroup> = {
  failed: {
    row: toolMessageStyles.rowFailed,
    header: toolMessageStyles.failedHeader,
    summary: toolMessageStyles.failedSummary,
    body: toolMessageStyles.failedBody,
  },
  running: {
    row: toolMessageStyles.rowRunning,
    summary: toolMessageStyles.runningSummary,
  },
  repairing: {
    row: toolMessageStyles.rowRepairing,
    header: toolMessageStyles.repairingHeader,
    summary: toolMessageStyles.repairingSummary,
    body: toolMessageStyles.repairingBody,
    icon: toolMessageStyles.repairingIcon,
  },
  success: { summary: toolMessageStyles.successSummary },
};
