/**
 * Re-export platform-neutral visibility authority (kept for stable web import paths).
 */
export {
  resolveAgentSyncActionVisibility,
  type AgentSyncActionVisibility,
} from "../agentSyncActionVisibility";
